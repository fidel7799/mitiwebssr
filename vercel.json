{
  "version": 2,
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "api/server.mjs",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "dist/mitiwebssr/browser/**",
          "dist/mitiwebssr/server/**"
        ]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/server"
    }
  ]
}
